Campaigns can be **copied between projects**, allowing you to reuse configurations without recreating them from scratch. This is especially useful when moving campaigns from staging to production or when standardizing setups across teams.

When copying a campaign, Voucherify recreates its configuration in the target project and lets you decide whether to reuse existing resources or map them to ones already available in the destination environment.