---
title: "Succeeded"
description: ""
openapi: "/openapi-events/events-redemption.json webhook EVENTS.REDEMPTION.SUCCEEDED"
---

The `EVENTS.REDEMPTION.SUCCEEDED` event indicates that the redemption process was successful. It confirms that the associated voucher codes and promotional tiers have been effectively applied.

A **redeemable stack** sends **one `redemption.succeeded` webhook for each redeemable**, plus **one extra webhook** for the whole stack.

<Info>
**Example**

5 redeemables → 6 `redemption.succeeded` webhooks
</Info>

This event is used in a webhook configured in Project settings in Voucherify dashboard.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>