---
title: "Expiration changed"
description: "Event data sent for a Loyalty v2 EXPIRATION_CHANGED transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook EXPIRATION_CHANGED"
keywords: ["loyalty v2", "workflow", "webhook", "tier expiration changed", "tier transaction"]
---

The `EXPIRATION_CHANGED` event indicates that a Loyalty v2 tier assignment expiration date changed.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Tier transactions** group and the `EXPIRATION_CHANGED` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data` for a transaction with the `APPROVED` status. A workflow can filter the trigger by transaction status. The matched status is available in `data.transaction.status`; it does not create a separate event type.

`details.tier_from` is `null`. `details.tier_to` is the current tier ID. `details.expires_at` is the new expiration timestamp, or `null` when expiration is cleared.
