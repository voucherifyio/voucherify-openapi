---
title: "Evaluation refreshed"
description: "Event data sent for a Loyalty v2 EVALUATION_REFRESHED transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook EVALUATION_REFRESHED"
keywords: ["loyalty v2", "workflow", "webhook", "tier evaluation refreshed", "tier transaction"]
---

The `EVALUATION_REFRESHED` event indicates that a Loyalty v2 tier evaluation was refreshed without changing the member's tier.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Tier transactions** group and the `EVALUATION_REFRESHED` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data` for a transaction with the `APPROVED` status. A workflow can filter the trigger by transaction status. The matched status is available in `data.transaction.status`; it does not create a separate event type.

`details.tier_from` and `details.start_at` are `null`. `details.tier_to` is the current tier ID.
