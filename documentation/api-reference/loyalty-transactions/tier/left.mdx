---
title: "Left"
description: "Event data sent for a Loyalty v2 LEFT transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook LEFT"
keywords: ["loyalty v2", "workflow", "webhook", "tier left", "tier transaction"]
---

The `LEFT` event indicates that a member left a Loyalty v2 tier.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Tier transactions** group and the `LEFT` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

Workflows send this transaction in the `data.trigger` object. The example uses the `APPROVED` status. A workflow can filter the trigger by transaction status. The matched status is available in `data.trigger.transaction.status`; it does not create a separate event type.

`details.tier_to` is `null`. `details.tier_from` is the left tier ID. `transaction.tier_id` may also be `null`.
