---
title: "Points spent on order"
description: "Event data sent for a Loyalty v2 POINTS_SPENT_ON_ORDER transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook POINTS_SPENT_ON_ORDER"
keywords: ["loyalty v2", "workflow", "webhook", "points spent on order", "card transaction"]
---

The `POINTS_SPENT_ON_ORDER` event indicates that points were spent on an order from a Loyalty v2 card.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Card transactions** group and the `POINTS_SPENT_ON_ORDER` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

Workflows send this transaction in the `data.trigger` object. The example uses the `APPROVED` status. A workflow can filter the trigger by transaction status: `PENDING`, `PROCESSING`, `APPROVED`, or `REJECTED`. The matched status is available in `data.trigger.transaction.status`; it does not create a separate event type.

Paying with points also creates a separate `PAY_WITH_POINTS` order transaction. That payload is not included in this event. Use the [Pay with points event](/api-reference/loyalty-transactions/order/pay-with-points) to get details about the order transaction.
