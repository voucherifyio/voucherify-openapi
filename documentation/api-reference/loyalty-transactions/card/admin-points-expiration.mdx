---
title: "Admin points expiration"
description: "Event data sent for a Loyalty v2 ADMIN_POINTS_EXPIRATION transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook ADMIN_POINTS_EXPIRATION"
keywords: ["loyalty v2", "workflow", "webhook", "admin points expiration", "card transaction"]
---

The `ADMIN_POINTS_EXPIRATION` event indicates that a points bucket was manually expired on a Loyalty v2 card.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Card transactions** group and the `ADMIN_POINTS_EXPIRATION` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data` for a transaction with the `APPROVED` status. A workflow can filter the trigger by transaction status. The matched status is available in `data.transaction.status`; it does not create a separate event type.
