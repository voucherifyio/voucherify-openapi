---
title: "Pending points activated"
description: "Event data sent for a Loyalty v2 PENDING_POINTS_ACTIVATED transaction handled by Workflows"
openapi: "/openapi-events/events-loyalty-transaction.json webhook PENDING_POINTS_ACTIVATED"
keywords: ["loyalty v2", "workflow", "webhook", "pending points activated", "card transaction"]
---

The `PENDING_POINTS_ACTIVATED` event indicates that pending points were activated on a Loyalty v2 card.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty transaction** is configured with the **Card transactions** group and the `PENDING_POINTS_ACTIVATED` transaction type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

Workflows send this transaction in the `data.trigger` object. The example uses the `APPROVED` status. A workflow can filter the trigger by transaction status: `PENDING` or `APPROVED`. The matched status is available in `data.trigger.transaction.status`; it does not create a separate event type.

This event follows a `PENDING_POINTS_ADDED` transaction for the same pending points. Use the [Pending points added event](/api-reference/loyalty-transactions/card/pending-points-added).
