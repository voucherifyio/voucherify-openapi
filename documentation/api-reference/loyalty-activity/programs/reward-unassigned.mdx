---
title: "Program reward unassigned"
description: "Event data sent for a Loyalty v2 vl.program.reward.unassigned activity handled by Workflows"
openapi: "/openapi-events/events-loyalty-activity.json webhook vl.program.reward.unassigned"
keywords: ["loyalty v2", "workflow", "webhook", "program reward unassigned", "loyalty activity"]
---

The `vl.program.reward.unassigned` event indicates that a reward was unassigned from a Loyalty v2 program.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>

This event can trigger a workflow when **Loyalty activity** is configured with the **Programs** group and the `vl.program.reward.unassigned` activity type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data`. It contains snapshots recorded with the activity. It is not wrapped in an `activity` property.

This operation also creates a separate `vl.reward.unassigned` activity. Use the [Reward unassigned event](/api-reference/loyalty-activity/rewards/unassigned) to handle that event type.
