---
title: "Program updated"
description: "Event data sent for a Loyalty v2 vl.program.updated activity handled by Workflows"
openapi: "/openapi-events/events-loyalty-activity.json webhook vl.program.updated"
keywords: ["loyalty v2", "workflow", "webhook", "program updated", "loyalty activity"]
---

The `vl.program.updated` event indicates that a Loyalty v2 program was updated.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty activity** is configured with the **Programs** group and the `vl.program.updated` activity type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

Workflows send this activity in the `data.trigger` object.
