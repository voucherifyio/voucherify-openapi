---
title: "Tier updated"
description: "Event data sent for a Loyalty v2 vl.tier_structure.tier.updated activity handled by Workflows"
openapi: "/openapi-events/events-loyalty-activity.json webhook vl.tier_structure.tier.updated"
keywords: ["loyalty v2", "workflow", "webhook", "tier updated", "loyalty activity"]
---

The `vl.tier_structure.tier.updated` event indicates that a tier in a Loyalty v2 tier structure was updated.

<Note>This page documents only the event. If you need more details about the webhook payload data that includes this event, go to [Introduction to webhooks](/api-reference/introduction-to-webhooks "Introduction to webhooks v2024-01-01") page.</Note>

This event can trigger a workflow when **Loyalty activity** is configured with the **Tier structures** group and the `vl.tier_structure.tier.updated` activity type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data`. It contains snapshots recorded with the activity. It is not wrapped in an `activity` property.
