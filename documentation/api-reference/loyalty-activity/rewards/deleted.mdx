---
title: "Reward deleted"
description: "Event data sent for a Loyalty v2 vl.reward.deleted activity handled by Workflows"
openapi: "/openapi-events/events-loyalty-activity.json webhook vl.reward.deleted"
keywords: ["loyalty v2", "workflow", "webhook", "reward deleted", "loyalty activity"]
---

The `vl.reward.deleted` event indicates that a Loyalty v2 reward was deleted.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty activity** is configured with the **Rewards** group and the `vl.reward.deleted` activity type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data`. It contains snapshots recorded with the activity. It is not wrapped in an `activity` property.
