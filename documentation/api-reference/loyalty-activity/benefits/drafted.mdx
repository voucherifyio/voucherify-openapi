---
title: "Benefit drafted"
description: "Event data sent for a Loyalty v2 vl.benefit.drafted activity handled by Workflows"
openapi: "/openapi-events/events-loyalty-activity.json webhook vl.benefit.drafted"
keywords: ["loyalty v2", "workflow", "webhook", "benefit drafted", "loyalty activity"]
---

The `vl.benefit.drafted` event indicates that a Loyalty v2 benefit was moved to draft.

<Note>
This page documents only the event data nested under `data`. See [Introduction to webhooks](/api-reference/introduction-to-webhooks) for the shared webhook structure, and [Workflows webhooks](/api-reference/workflows-webhooks) for the full Workflows webhook payload.
</Note>

This event can trigger a workflow when **Loyalty activity** is configured with the **Benefits** group and the `vl.benefit.drafted` activity type. If the workflow reaches a **Webhook callout** action, Voucherify sends a webhook to the action's target URL.

The example shows the content of `data`. It contains snapshots recorded with the activity. It is not wrapped in an `activity` property.
